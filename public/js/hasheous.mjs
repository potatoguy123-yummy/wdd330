const API_URL = '/api/hasheous';

export async function lookupMd5(md5sum) {
    console.log("lookup", md5sum);
    // todo
    return {
        found: true,
        name: "Potato Wars 3",
        description: "A war with potatoes"
    }
}
